{
  "name": "chat-client",
  "version": "2.0.0",
  "description": "Secure private chat client",
  "type": "module",
  "scripts": {
    "dev": "bunx --bun vite",
    "build": "bunx --bun vite build",
    "preview": "bunx --bun vite preview",
    "dev:npm": "vite",
    "build:npm": "vite build"
  },
  "dependencies": {
    "socket.io-client": "^4.7.0",
    "libsodium-wrappers": "^0.7.13"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "sass": "^1.70.0"
  }
}
